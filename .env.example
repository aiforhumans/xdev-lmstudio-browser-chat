# LM Studio OpenAI-compatible API base URL
# In LM Studio, start the local server on port 1234.
LM_STUDIO_BASE_URL=http://localhost:1234/v1

# Browser UI server port
APP_PORT=3000

# Optional default model.
# Leave empty and use the model dropdown from /v1/models.
DEFAULT_MODEL=
